# Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here

# GitHub OAuth (optional, for teacher/parent accounts)
GITHUB_ID=your-github-oauth-id
GITHUB_SECRET=your-github-oauth-secret

# Vercel KV (Redis) - for production
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# OpenAI API - REQUIRED for AI-powered game generation
# Get your API key from: https://platform.openai.com/api-keys
# Without this key, the app will use template-based generation only
# With this key, complex prompts (>100 characters) will use GPT-4 to generate complete HTML games
OPENAI_API_KEY=your-openai-key-here
